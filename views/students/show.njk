{% extends "layout.njk" %}


{% block content %}
    <div class="card">
        <section
            class="picture"
            style="background: url({{ students.avatar_url }}) no-repeat center center / cover"
        >        
        </section>

        <section class="details">

            <h3>About</h3>
            <div class="item ite-bord"">
                <div> Student </div>
                <div> {{ students.name }}</div>
            </div>
            <div class="item">
                <div> Email </div>
                <div> {{ students.email }}</div>
            </div>
            <div class="item">
                <div> Idade </div>
                <div> {{ students.birth }} Anos</div>
            </div>
            <div class="item">
                <div> Sexo </div>
                <div> {{ "Masculino" if students.gender == "M" else "Feminino"}} </div>
            </div>
            <div class="item">
                <div> Ano Escolar </div>
                <div> {{ students.escolaridade }}º Ano </div>
            </div>
            <div class="item">
                <div> Tipo de Aula </div>
                <span>
                    {{ students.type_learn }}
                </span>
            </div>
            <div class="item">
                <div> Carga Horária </div>
                <div>                    
                    <span>{{ students.time }} Horas</span>            
                </div>
            </div>
            <div class="item">
                <div> Desde </div>
                <div> {{ students.create_at }}</div>
            </div>
            <a href="/students/{{ students.id }}/edit" class="button">Editar</a>
        </section>

    </div>
{% endblock %}